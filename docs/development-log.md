# 开发日志

## 2026-09-13：接触尝试评估层与AirSim阶段入口

- 新增 `python/attempt_assessment.py`，将一次视觉最终接触尝试独立标记为 `TRACKING`、`TEMPORARILY_LOST`、`UNCONFIRMED`、`FAILED` 或 `CONTACT_CONFIRMED`，并输出 `CONTINUE`、`REACQUIRE`、`HANDOFF_ELIGIBLE`、`COMPLETE` 等动作建议。该层不发送飞行命令；新 `BalloonTarget` 碰撞仍是唯一完成依据。
- `python/run_visual_fallback.py` 保留默认的预设 A 漏击回归路径，同时新增 `--attempt-mode observation`。观测模式在A最终接触段记录观测新鲜度、时序跟踪、超时/遮挡边界及接替资格，再进入现有通道清空和B补位流程；尚未进行UE飞行验收。
- 任务报告增加 `evaluation`、`truth_access`、`attempt_policy`、`attempt_assessments` 和带来源/原因的关键事件字段；批量行保留评估模式、策略编号、A尝试状态和失败原因，便于把故障注入与候选自主模式分开统计。
- `python/launch_demo.py` 和 `python/batch_validate.py` 暴露 `--attempt-mode`，默认值不改变历史基线。`python/README.md` 与 `docs/research-plan-20260913/AirSim功能与测试清单.md` 增加运行和验收说明。
- 重新运行 `& .venv\\python.exe -W error::ResourceWarning -m unittest discover -s python -p 'test_*.py'`：**77项通过，0失败**；另以 `py_compile` 检查新增/修改的Python入口通过。随后完成两次默认基线和一次观测模式AirSim运行；本轮未训练模型，不能把77项离线通过写成飞行成功率。

下一步：检查这两类报告的逐帧轨迹和状态机事件，再按T01–T12补齐真实未完成、自然遮挡、恢复、接替、双失败和多种子批量测试。

本轮AirSim验收已完成两次默认基线和一次观测模式：`mission_visual_fallback_20260913_120408_087100`（默认）与 `mission_visual_fallback_20260913_120849_614468`（观测）均为 PASS，最终状态为 `HIT_CONFIRMED`，命中机为 DroneB，双机均完成落地并释放控制。默认基线的 A 漏击事件来源为 `test_injection`；观测模式的 A 尝试经过 `TRACKING → TEMPORARILY_LOST → UNCONFIRMED/HANDOFF_ELIGIBLE → FAILED`，原因 `track_lost_before_authoritative_contact`，随后 B 接替。两次 B 最终段均为 `visual_gate_coordinate_contact`，即视觉门控后使用碰撞必需的坐标收尾；没有产生纯视觉命中结论。

## 2026-09-08：双机检查与气球场景

- AirSim 实际返回 `DroneA` 和 `DroneB`，均可读取状态和世界坐标。
- 新增 `python/setup_scene.py`，统一检查、创建气球、采集截图和记录 JSON 日志。
- `configs/balloon.json` 配置红色静态气球：世界 NED `(0, 0, -3)` 米，缩放 `(1.2, 1.2, 1.5)`，分割 ID 42，系绳长度约 1.8 米。
- 在 Blocks 实际运行成功，并查看总览截图确认气球与两架无人机同时存在。重复运行复用同名对象。
- 气球周围 2 米立方体、0.1 米分辨率的碰撞查询检测到 1285 个阻挡体素（8000 个采样体素）；这说明该区域存在阻挡几何，尚未用无人机实际接触验证命中。
- 修正 AirSim 分割编号读取时名称大小写问题：getter 必须使用小写名称，setter 返回成功不代表原大小写的 getter 可以查到。
- `scripts/start-scene.ps1` 提供启动和重建入口，已有 RPC 服务时直接复用。
- `.gitignore` 改为只忽略根目录个人 `settings.json`，保留 `configs/settings.json` 可供版本管理。

### 使用与边界

运行 `scripts/start-scene.ps1`。每次输出在 `logs/scene_<时间>/`，含总览图、双机前视图、碰撞采样、文字日志与 JSON 报告。日志和贴图是本地运行产物，不提交 Git。

气球在当前运行场景中生成，尚未保存为 UE 地图资产；关卡重启后重新执行脚本即可恢复。气球不受重力影响，暂不模拟破裂或摆动。双机世界坐标与各自飞控局部 NED 坐标不同，后续导航必须换算，不能直接混用。

### 接下来

1. 实现接触判定：记录碰撞对象、时刻和接近过程；区分气球接触、其他碰撞及未命中。
2. 完成 A 接近、B 等待的协调流程，再验证 A 未命中时 B 补位。
3. 加入可靠降落、失败清理及飞行轨迹记录；现有 smoke 脚本的下降后解除武装不等同于经过验证的自动降落。

## 2026-09-08：第 1、2 步飞行演示完成

新增 `python/run_approach_demo.py` 和 `scripts/run-approach-demo.ps1`。双机升至世界 NED Z=-3 米，A/B 分别在 (-5,-4,-3)、(-5,4,-3) 待命；随后 A 低速移至 (-2,0,-3)，B 保持待命。记录坐标原点换算、四个阶段的实际位置及逐帧遥测。屏幕用青色标记 A、黄色标记 B。

实际验收：`logs/approach_20260908_174119_647537/report.json` 首次完整通过；随后 `logs/approach_20260908_174323_332771/report.json` 从暂停的空中状态重播通过。后一次 A 最终距离气球中心 1.999 米，B 在接近和保持阶段的最大偏移 0.051 米，全程最小双机间距 4.946 米，无新的碰撞事件。最终位置稳定保持 10 秒。

调试记录保留：首次位置指令停在距目标约 0.39 米处，严格到位检查超时；分段速度修正方案出现高度偏移和接近超时；另一次恢复测试发生地面碰撞，立即暂停。改用连续、限速的位置误差反馈，且已持有 API 控制时不重复请求控制（SimpleFlight 的 requestApiControl 会将目标重置为遥控输入）。地面碰撞的具体因果未单独隔离验证。以上失败日志保留，未作为通过结果。

演示成功或失败后会暂停仿真，保留现场供查看；暂停不代表已降落，飞机仍保持解锁。再次运行演示脚本会恢复并重播。没有实现气球命中、B 补位和自动降落。当前路线限于已检查的 Blocks 空旷区域；不是通用避障规划器。模板默认观察模式改为 Manual，当前窗口已是自由相机；重装配置后下次启动生效。

## 2026-09-08：真实接触、自动补位与返回降落

上述接近演示之后，新增完整实验 `run_balloon_mission.py`、证据判定 `hit_judge.py`、离线测试 `test_mission_logic.py`、汇总工具 `summarize_experiments.py`。复用 `mission_state.py`，将状态转换绑定到实际路线完成及新碰撞事件。新增双击入口 `Run-Balloon-Demo.cmd`、PowerShell 场景选择入口和中文 `docs/demo-guide.md`。

| 实测场景 | 结果 | 最小双机间距 | 最大等待偏移 | 降落 |
|---|---|---|---|---|
| A 直接接触：`mission_a_hit_20260908_175914_857837` | PASS，A 与 BalloonTarget 新碰撞 | 4.911 米 | 0.043 米 | 接地稳定后解除武装、释放控制 |
| A 预设偏离、B 补位：`mission_fallback_20260908_175702_569549` | PASS，B 与 BalloonTarget 新碰撞 | 4.912 米 | 0.042 米 | 接地稳定后解除武装、释放控制 |

补位实测：17:57:22 判为 A 偏离路线完成，17:57:32 通道清空并进入 B 补位，17:57:48 收到 B 接触目标事件。退出通道约需 10.9 秒；清空到派发的应用日志事件处于同一计时刻度，不代表零通信延迟或瞬间命中。新版本报告单独记录这两种时间间隔及预设偏离的最小中心距离。

气球和系绳只在真实新碰撞确认后移除。已查看 contact.png 和 after_hit.png，确认接触前目标存在、之后消失。目标移除后两机返回各自已检查的地面位置。

第一次 A 实验 `mission_a_hit_20260908_175503_051340` 虽确认命中，但因 SimpleFlight 接地后仍报告 Flying 而未通过降落枚举检查，整体标为 FAIL；汇总保留该结果。随后依据源码定位到降落检测依赖 RC 电机输出，改为用本轮 Ground 接触、已知平地坐标范围及连续至少 2 秒低速确认接地，再解除武装。原始 landed 枚举仍写入报告；不声称飞控枚举已可靠。该规则仅用于当前 Blocks 平地降落点。

5 个离线测试通过，覆盖旧碰撞拒绝、无碰撞拒绝、地面/系绳/相似名称拒绝、A 成功后不启动 B、未进入让路后状态不得补位、B 超时进入失败状态。运行报告、逐帧日志、截图均保留在 logs；汇总 CSV 同时包含 FAIL 和 PASS。最终现场两机已解除武装、API 控制已释放，仿真暂停，气球已移除。

后续可以推进摄像头识别气球、参数化实验、更多位置与干扰测试。当前两个成功样例只证明这两条固定路线可运行，不代表通用成功率或障碍物场景可靠性。

## 2026-09-08：框架变更后的实跑

新增基础框架后立即执行 12 个离线测试、场景恢复和 fallback 实跑。`mission_fallback_20260908_220413_611369` 通过：A 未命中，B 收到补位派发并与 `BalloonTarget` 产生新碰撞，气球移除，两机接地后解除武装。之后的每次改动按同一顺序验证：离线测试 → Blocks 场景检查 → 至少一条实际任务路线 → report/画面检查。

## 2026-09-08：修复用户双击启动被拦截

用户截图表明 Windows PowerShell 在执行 `.ps1` 前报 PSSecurityException，仿真未启动。将 `Run-Balloon-Demo.cmd` 改为直接调用 `.venv/python.exe` 和 `python/launch_demo.py`，提供同样的启动检查、场景恢复、实验选择与汇总流程，不修改系统执行策略。`--check` 可从实际 CMD 入口验证依赖及 RPC 状态，不启动或操纵仿真。旧 PowerShell 入口仍保留，双击入口不再依赖它。

## 2026-09-08：摄像头红色气球识别基础版

新增 `balloon_detector.py`、`check_vision.py` 和 4 个离线检测测试，启动菜单扩展为 1/2/3。菜单 3 完成双机就位、暂停、读取两架前视相机、保存有/无目标对照并显示图像。原有 5 个任务逻辑测试加 4 个检测测试，共 9 个通过。

初次实拍（vision_20260908_182422）发现：B 的气球位于左画面边缘，原始完整轮廓规则漏检它，却把机身约 100 像素的红色斑点列为候选。提高最小面积并允许带 truncated 标记的边缘候选后，在 `vision_20260908_182556` 验证通过：A/B 各有一个目标候选，B 标为截断；移走目标后的两幅实拍图均无候选。验证后恢复气球原位置。已人工查看四格对照图。

标注框/质心来自 RGB 像素；到观察点的飞行仍用模拟器坐标，识别没有接管飞控。未进行实时视觉跟踪、三维距离估计或新环境泛化验证。当前验证集很小，不能称为识别准确率 100%。菜单 3 与完整接触演示的结束状态不同：飞机仍在空中、仿真暂停；说明中明确区别。

追加验证：首次完整视觉启动暴露了 B 相机默认方向背向目标，因而不能要求两台相机都必须看到气球。验证规则已改为至少一台相机检测到目标、移出目标后两台均无候选；每台相机的结果都保留。配置模板恢复为经验证的默认前视方向，视觉检测不再修改相机姿态。当前运行中的 UE 窗口已恢复默认相机姿态，验证报告 `vision_20260908_214810` 通过；现场保持暂停，A 位于气球旁供查看。

## 2026-09-08：基础软件框架拆分

新增 `configs/mission.json`、`mission_config.py`、`target_provider.py` 和 `coordinator.py`。配置、目标感知和任务仲裁现在有独立接口：GroundTruth 与 Camera 都产生 `TargetObservation`，Coordinator 只负责 A→miss→corridor clear→B 的状态，不直接发飞行命令。12 个离线测试通过（原有 9 个加框架 3 个）。现有运行器暂时保持固定路线，下一步接入配置对象和可选视觉制导环路。

## 2026-09-08：任务运行器接入统一任务配置

`run_balloon_mission.py` 已改为读取 `configs/mission.json`：双机列表、目标名称、停机位、集结位、接近点、最小间距、等待漂移和接触超时不再分散写死。配置对象带校验和可审计的 `to_dict()` 输出，任务报告同时记录配置内容和 `mission_config.py` 源码哈希。修改后 12 个离线测试通过；`mission_fallback_20260908_221533_173754` 实跑通过，A 按预设路线偏离，通道清空后 B 完成目标碰撞，两机稳定降落并解除武装。

## 2026-09-08：协调器接入与双场景回归

运行器正式使用 MissionCoordinator 作为任务状态来源，开始、命中、漏击、让路后补位及超时均通过协调器处理。接触动作必须匹配当前派发飞机。异常中止统一记为 FAILED，报告保留异常类型、失败阶段、中止前状态；暂停失败单独记录 manual_stop_required。失败时也保留已采集的视觉记录。源码指纹新增 coordinator.py。

17 项离线测试通过，新增命中后禁止补位、B 超时、命中后执行故障中止、未派发飞机不得进入接触流程、运行器事件派发验证。

| 实跑记录 | 结果 | 最小间距 | 最大等待偏移 |
| --- | --- | --- | --- |
| mission_a_hit_20260908_223016_556107 | PASS，A 命中，B 无补击，两机降落解除控制 | 4.9012 m | 0.0469 m |
| mission_fallback_20260908_223118_377973 | PASS，A 预设漏击，B 补位命中，两机降落解除控制 | 4.9014 m | 0.0494 m |

使用 launch_demo.py --scenario both 连续运行，中间成功恢复气球。补位轮漏击到派发约 10.938 秒，包含 A 让路时间；清空到派发记为 0 秒，仅代表日志计时分辨率。已查看 A 命中后截图，气球消失、双机仍在场景中。experiment_summary.csv 已更新，保留历史失败记录。最终仿真暂停，两机已降落解除控制。

验收边界：本轮完成第 10 阶段协调器接入及第 11 阶段部分失败记录改进；故障现场完整复位、真实断连/超时注入、批量稳定性仍未验收。根目录尚未初始化 Git；本次仅保存本地代码、文档和日志，没有上传。logs 已被 .gitignore 排除，开发日志可随代码版本保存。

## 2026-09-08：安全整轮复位入口

新增 `python/reset_mission.py` 和启动菜单选项 5。复位会检查两架飞机都处于地面静止状态，再取消上一轮任务、解除 API 控制、调用 `setup_scene.py` 重建气球；空中状态会拒绝操作并写入失败报告。由于 Blocks 的世界坐标落地高度约为 z=1.68，复位采用地面状态或 z=1.50～1.90 且速度低于 0.08 m/s 的组合判据。首次试运行发现 AirSim Python 接口名称为 `cancelLastTask`，已修正后通过。

实测 `reset_20260908_223905_567318`：PASS；场景报告 `scene_20260908_223906_393062`：PASS；气球恢复到 `[0, 0, -3]`，两架飞机控制已释放。复位报告、reset.log 和场景报告均保留在对应 logs 子目录。

## 2026-09-08：异常路径验证

本轮按“离线逻辑 → UE/RPC 环境 → 场景完整性”的顺序测试。离线共 21 项通过：

| 测试类别 | 原理 | 结果 |
| --- | --- | --- |
| A 命中终止 | 命中后状态进入 `HIT_CONFIRMED`，状态机没有补位转换 | PASS |
| B 补位前置条件 | A 必须先报告漏击，且必须先收到 `corridor_clear`，B 才能派发 | PASS |
| 补位超时 | B 已派发但超过接触时限，状态进入 `FAILED` | PASS |
| 命中证据 | 只接受新时间戳、碰撞标志为真、对象名精确等于 `BalloonTarget` | PASS |
| 旧碰撞/擦碰/地面碰撞 | 旧时间戳、非碰撞、系绳、相似名称均不能计为命中 | PASS |
| 配置异常 | 最小间距为零或两个待命点重合时，在起飞前拒绝配置 | PASS |
| 未派发飞机接触 | 运行器检查当前协调器派发对象，未派发的飞机不能进入接触流程 | PASS |

UE 实测检查也通过：AirSim RPC 端口可用、UE 进程存在、DroneA/DroneB 均存在，气球对象位置为 `[0,0,-3]`，目标区域有 1285 个阻挡体素。完整记录保存在 `logs/verification_20260908_224411/`，包括测试输出、环境检查、场景检查和汇总报告。

结论：状态机、碰撞裁判和配置防护已经能确定性拒绝本轮覆盖的异常输入；当前尚未做真实网络断开或飞行中人为超时注入，因此“通信异常恢复”和“空中故障复位”仍是下一轮实测项目，不能由本轮离线测试替代。

## 2026-09-08：双场景真实回归测试

重新执行 `python/launch_demo.py --scenario both`，用于验证协调器接入后的真实双机行为。每一轮先由 `setup_scene.py` 恢复气球，再执行飞行任务，结束时检查目标碰撞、双机分离、待命机漂移、地面接触和解除 API 控制。

| 场景 | 实际状态链 | 结果 |
| --- | --- | --- |
| A 命中 | `READY → A_APPROACH → HIT_CONFIRMED`；只记录 DroneA 与 `BalloonTarget` 的新碰撞 | PASS；最小间距 4.9024 m，最大等待偏移 0.0627 m |
| A 漏击/B 补位 | `READY → A_APPROACH → A_MISSED → B_APPROACH → HIT_CONFIRMED`；A 先返回集结位后才派发 B | PASS；漏击到派发 11.141 s，最小间距 4.9023 m，最大等待偏移 0.0492 m |

记录目录：`mission_a_hit_20260908_230111_691317`、`mission_fallback_20260908_230213_801829`。两轮均完成降落、解除控制，仿真暂停；`experiment_summary.csv` 已追加结果。

结论：固定 Blocks 场景下，双机协调和补位路径连续回归通过；A 命中不会误派 B，A 漏击也不会在通道未清空前派 B。当前结论只覆盖固定目标、固定航点和预设漏击，尚不代表动态目标、随机故障或视觉闭环的成功率。

## 2026-09-08：基础版测试收尾

真实销毁 `BalloonTarget` 和系绳后启动任务，运行器在 `PREFLIGHT` 阶段检测到目标位置无效，拒绝起飞并写入预期失败报告 `mission_a_hit_20260908_230753_260954`。随后 `setup_scene.py` 成功恢复目标，场景报告为 `scene_20260908_230754_231922`。再次执行 21 项离线测试、启动器 RPC 检查和安全复位，全部通过；最终复位记录为 `reset_20260908_230805_948496`。

基础版完整过程、原理、结果和适用边界整理到 `docs/base-v1-test-report.md`。当前满足进入第 12 阶段固定场景批量验证的条件。

## 2026-09-08：批量验证器基础版

新增 `python/batch_validate.py`，按“恢复场景 → 执行任务 → 读取该轮 report → 写入批次汇总”的顺序循环运行。支持选择 `a_hit`、`fallback` 和重复次数；批次报告记录任务配置哈希、每轮日志目录、命中飞机、降落验证、最小间距、最大等待偏移和漏击到派发时间。任一轮失败立即停止，避免复用异常现场。

新增 2 项汇总算法测试，完整离线测试增加到 23 项并全部通过。小批次 `batch_20260908_231309_780858` 完成 1 轮 A 命中实飞：PASS；命中飞机 DroneA；最小双机距离 4.8986 m；最大等待偏移 0.0510 m；两机完成降落并解除控制。批量框架已经可用，下一步扩展为 A/B 各多轮并统计稳定性。

## 2026-09-08：首轮 3×2 批量稳定性测试

执行 `batch_validate.py --repeats 3 --scenarios a_hit fallback`，共完成 6 次真实飞行。结果 6/6 PASS：三轮 A 命中均由 DroneA 命中且未派发 B；三轮补位均按 A 漏击、通道清空、派发 B、B 命中的顺序完成。所有轮次均通过降落验证并释放 API 控制。

批次 `batch_20260908_231640_408133` 的最小双机距离下界为 4.8938 m，最大等待机漂移为 0.0604 m，三轮补位的平均漏击到派发时间为 11.0573 s。该 6 次样本在固定坐标和固定漏击偏差下成功率为 100%，仅作为功能回归样本，样本量不足以推断广泛场景可靠性。

已设置当前任务在约 5 小时内每 30 分钟继续一次，共 10 次后续推进。每次先检查上一轮状态，再进行有边界的构建或测试，并持续更新本日志；发现空中异常或故障现场时停止后续飞行。

## 2026-09-08：跨批次统计汇总

新增 `python/analyze_batches.py`，读取所有已完成 `batch_report.json`，按任务日志目录去重并按场景汇总。输出 `logs/batch_summary.json` 和 `logs/batch_summary.csv`，统计每类运行数、通过率、最小安全间距、最大待命漂移、补位派发延迟均值及样本标准差。新增 1 项统计测试，完整测试数增至 24 项并全部通过。

当前两个批次共包含 7 个唯一运行：A 命中 4/4 PASS，补位 3/3 PASS；全体最小间距 4.8938 m，最大待命漂移 0.0604 m；补位派发延迟均值 11.0573 s，样本标准差 0.0858 s。样本仍来自同一固定目标和同一预设漏击轨迹，只能作为重复性基线。

## 2026-09-09：漏击偏差参数化

批次报告确认完成后检查运行环境：UE 进程和 AirSim RPC 均已关闭，因此本轮不启动飞行测试。将原先写死在运行器中的 `y=-2 m` 漏击路线改为 `configs/mission.json` 的 `test_parameters.deliberate_miss_offset_y_m`。运行器以目标 Y 坐标为基准叠加该偏差，并将实际偏差写入 `planned_miss` 事件，便于后续按偏差做参数扫描。

配置校验要求偏差绝对值至少 1.5 m，防止所谓“预设漏击”实际穿过当前固定气球碰撞体。新增不安全偏差拒绝测试，完整离线测试增至 25 项，全部通过。由于 UE 已关闭，本次只完成代码与离线验证；下一次 UE/RPC 可用时需要先运行一轮 fallback 实飞确认该配置路径。

UE 重新启动后完成补测：`mission_fallback_20260909_081603_272974` PASS。`planned_miss` 明确记录 `offset_y_m=-2.0`；A 漏击并退出通道后，约 12.344 s 派发 B，B 与 `BalloonTarget` 产生新碰撞；最小双机距离 4.9015 m，最大等待偏移 0.0512 m，两机落地并解除控制。

运行器新增 `--miss-offset-y` 临时覆盖参数，覆盖值同样经过 `abs(offset) >= 1.5 m` 校验并写入报告。使用 `-2.5 m` 实飞验证 `mission_fallback_20260909_081850_065565`：PASS；A 按新偏差漏击并让路，B 补位命中，两机降落解除控制。批量器也已支持 `--miss-offset-y`，可为不同偏差分别生成可比较的批次报告。

## 2026-09-09：-2.5 m 漏击偏差批次

确认 UE、AirSim RPC 和双机状态可用后，执行 `batch_validate.py --repeats 2 --scenarios fallback --miss-offset-y -2.5`。两轮均按配置偏差完成 A 漏击、通道清空、B 补位命中和双机降落，结果 2/2 PASS。批次最小双机距离 4.9007 m，最大等待漂移 0.0532 m，平均漏击到派发时间 10.9610 s。批次记录为 `batch_20260909_084326_632135`。

重新汇总后，批量框架中共有 9 个唯一实飞样本：A 命中 4/4 PASS，fallback 5/5 PASS。该对比说明 `-2.5 m` 偏差在当前固定 Blocks 场景中仍能稳定触发补位流程，但不能外推到其他目标位置或障碍布局。

## 2026-09-09：+2.0 m 反方向漏击偏差批次

执行 `batch_validate.py --repeats 2 --scenarios fallback --miss-offset-y 2.0`，测试 A 从目标另一侧漏击。两轮均完成 A 漏击、返回集结位、B 补位命中和双机降落，2/2 PASS。批次 `batch_20260909_091328_853499` 的最小双机距离为 4.8911 m，最大等待漂移为 0.0504 m，平均漏击到派发时间为 14.1095 s。

与 `-2.5 m` 批次平均 10.9610 s 相比，`+2.0 m` 的让路时间明显更长，因为 A 漏击点位于目标正 Y 侧，返回负 Y 集结位的路线更长。当前安全距离仍远高于 2.0 m 阈值。跨批次累计 11 个唯一运行：A 命中 4/4、fallback 7/7；方向差异已经验证，但每个新增偏差只有两轮，统计结论仍是探索性的。

## 2026-09-09：按漏击偏差分组统计

原跨批次汇总只按 `scenario` 聚合，会把不同漏击偏差混为一个均值，无法解释方向带来的延迟差异。`analyze_batches.py` 已改为按 `(scenario, miss_offset_y_m)` 分组；新批次从顶层报告读取偏差并写入汇总。旧批次创建时尚未记录该字段，因此保留为 `null`，不擅自回填。

新增偏差分组测试后，完整离线测试为 27 项并全部通过。更新后的 `batch_summary.json/csv` 分别显示：旧默认批次 11.0573 s、`-2.5 m` 批次 10.9610 s、`+2.0 m` 批次 14.1095 s；各组均保留独立样本标准差和安全极值。

## 2026-09-09：可读批量测试报告

`analyze_batches.py` 新增 Markdown 输出 `logs/batch_summary.md`，将不同场景和漏击偏差的通过率、安全极值、派发延迟及标准差整理为可直接阅读的表格，并在报告底部声明固定 Blocks 场景的适用边界。新增渲染测试后，完整离线测试为 28 项并全部通过。JSON 和 CSV 输出保持不变，便于后续程序分析和 Excel 查看。

## 2026-09-09：目标位置参数化与首次偏移实飞

`setup_scene.py`、`run_balloon_mission.py` 和 `batch_validate.py` 新增统一的 `--target-offset X Y Z`。场景中的气球、系绳、碰撞采样区、任务期望目标坐标、接近点及撤离点共同平移；运行报告保存实际目标偏移。初始安全范围限制为水平 ±1.0 m、垂直 ±0.5 m，超出范围在飞行前拒绝。

执行 `batch_validate.py --repeats 1 --scenarios fallback --miss-offset-y -2.0 --target-offset 0.5 0 0`。气球实际恢复到 `[0.5,0,-3]`，A 完成漏击让路，B 在平移后的目标产生新碰撞，两机落地解除控制。批次 `batch_20260909_102536_282641` PASS：最小双机距离 5.2075 m，最大等待漂移 0.0537 m，漏击到派发 11.6720 s。

跨批次汇总器同步按目标偏移分组，防止不同位置的数据混合；新增目标位置分组测试后完整离线测试增至 30 项并全部通过。当前偏移位置仅完成 1 轮，是功能验证，不足以估计该位置的稳定成功率。

## 2026-09-09：位置矩阵运行器与 Y 偏移实飞

新增 `configs/validation_matrix.json` 和 `python/matrix_validate.py`。矩阵用命名案例管理目标偏移、漏击偏差和重复次数，可运行单个案例或完整矩阵；每个案例调用批量器，任一案例失败立即停止并保留报告。新增案例名称唯一性、位置边界和安全配置测试，完整离线测试增至 33 项并通过。

执行 `matrix_validate.py --case y_plus_05`，气球恢复到 `[0,0.5,-3]`。A 使用相对目标的 `-2.0 m` 偏差完成漏击和让路，B 在新位置命中目标，两机落地并解除控制。矩阵 `matrix_20260909_103736_577850`、批次 `batch_20260909_103737_350751` 均 PASS；最小双机距离 5.3174 m，最大等待漂移 0.0584 m，漏击到派发时间 11.5150 s。

## 2026-09-09：完整目标位置矩阵

先单独执行 `z_up_025`，气球上移 0.25 m 后补位命中和降落通过；随后不带筛选参数运行完整四案例矩阵。`matrix_20260909_104328_737903` 中中心、X+0.5 m、Y+0.5 m、Z上移0.25 m 四项全部 PASS，每项均重建目标、完成 A 漏击/B 补位并验证双机降落。

本次完整矩阵各案例的漏击到派发时间依次为 10.8910、11.7030、11.2190、11.0780 s；最小双机距离均高于 4.8985 m，最大等待漂移均低于 0.0560 m。跨批次累计 18 个唯一实飞样本。结果说明路线平移在当前受限范围内可工作，但每个位置样本仍很少，尚不能作为一般位置鲁棒性的结论。

## 2026-09-09：相机感知接口接入任务记录

任务运行器的视觉采集改为调用 `CameraTargetProvider.observe_with_frame()`，统一产生 `TargetObservation`，同时保留原图和检测结果用于标注。报告现在明确保存来源 `camera`、候选数量、像素中心、边界框、形状分数、归一化图像误差和裁剪标志。空图像或无法解码的图像产生未检测观测，不冒充目标。

新增空相机帧测试，并修正参数校验顺序，避免无效参数测试提前创建 AirSim socket。以 ResourceWarning 作为错误运行完整测试，34 项全部通过且无资源泄漏警告。实飞 `mission_a_hit_20260909_105332_696844` PASS：记录 55 组双机视觉观测，活动机在接近过程中有 5 帧检测到气球；命中并移除目标后最后观测为未检测到。最小双机距离 4.9007 m，最大等待漂移 0.0474 m，两机完成降落解除控制。

视觉接口目前只进入遥测与证据记录，不参与航点或命中决策；任务仍以模拟器坐标飞行，并以新碰撞事件作为命中裁判。

## 2026-09-09：连续多帧感知状态与阶段统计

新增 `perception_state.py`，每架飞机各自维护相机时序状态。默认连续 2 帧检测后进入 `DETECTED`；已建立目标后首次漏检进入 `TEMPORARILY_LOST`；连续 3 帧漏检进入 `LOST`；短暂丢失后再次检测可立即恢复。阈值由 `configs/mission.json` 管理并在载入时校验，角色固定记录为 `advisory_only`，不参与航点、补位派发或命中裁判。

新增 `analyze_perception.py`，按阶段和飞机汇总帧数、原始检测、三个跟踪状态及空图像次数，并在任务目录产生 `perception_summary.json` 和 `perception_summary.md`。离线测试增至 41 项，覆盖首次确认、短暂丢失、重新捕获、连续丢失、非法阈值和汇总输出，全部通过；同时启用 `ResourceWarning` 即失败，未发现资源警告。

实飞 `mission_a_hit_20260909_110824_435051` PASS：DroneA 与 `BalloonTarget` 产生新碰撞，最终状态 `HIT_CONFIRMED`；两机落地并解除控制；最小间距 4.9004 m，最大等待漂移 0.0273 m。共记录 55 组双机观测和 8 次状态转换，最终两机均为 `LOST`，符合气球命中后已被移除的现场。起飞和集结阶段也出现连续检测；当前没有这些阶段的标注图，不能直接认定为真阳性或误检，因此报告明确要求结合目标可见性与图像复核，不把阶段内 `DETECTED` 等同于真实识别成功。

阈值移入配置后的集成实飞 `mission_a_hit_20260909_111403_190492` 再次 PASS，报告明确记录 `confirm_frames=2`、`lost_frames=3`、`role=advisory_only`；56 组观测、8 次转换、无空图像，两机均落地并释放 API 控制。

## 2026-09-09：深度测距、残留接触修复与空中故障回收

`CameraTargetProvider` 新增 AirSim `DepthPerspective` 读取，在 RGB 目标框的内部区域计算有限正深度中位数。首次实飞 `mission_a_hit_20260909_111654_868463` 暴露两个真实问题：一是气球销毁过程中 Unreal 又上报一个新时间戳的残留接触，任务在撤离阶段误判异常；二是故障回收的地面高度判据没有覆盖该落点。失败现场按设计暂停并完整保留。

命中裁判现会在已有权威命中证据且状态为 `HIT_CONFIRMED` 时记录并忽略同一目标的销毁残留接触，不能再次计分。新增 `recover_airborne.py`：读取两机状态，必要时取消旧任务、共同返回停机位、降落，随后依据 AirSim 落地标志或已知地面高度并结合低速证据解除武装和 API 控制。第一次回收 `recovery_20260909_111932_688884` 因高度判据过窄而 FAIL，但两机实际已落地；修正后 `recovery_20260909_112226_206256` PASS。失败记录均保留。

残留接触修复实飞 `mission_a_hit_20260909_112243_829298` PASS。深度字段当时仍为空；直接探测确认深度相机确实输出 256×144 浮点图，而 RGB 为 640×360，原实现错误地直接使用 RGB 像素框。加入分辨率比例变换后，`mission_a_hit_20260909_112647_306968` PASS：DroneA 接近阶段取得 6 个深度样本，中位数 2.9209 m；DroneB 接近阶段取得 12 个样本，中位数 5.1445 m；无空图像，两机最终落地、解除武装并释放控制。完整离线测试现为 44 项通过。

深度和 RGB 是分别请求，尚未达到严格同步；框内中位数也可能混入背景或机体表面。因此本轮只证明当前 Blocks 场景能取得有限正距离，并未证明三维定位精度。感知仍不控制飞行，碰撞仍是命中权威证据。

阶段统计补充首末深度变化。上述通过任务中，DroneA 在 `APPROACH` 的首个有效深度约 5.09 m、最后一个约 1.23 m，变化 -3.86 m，符合接近目标的方向；DroneB 同期约 5.14 m 且变化接近 0，符合待命状态。该趋势是内部一致性检查，并非与外部测量真值比较后的精度标定。

## 2026-09-09：视觉制导建议层（不接管飞控）

新增 `guidance_advisor.py`，以多帧跟踪状态、归一化图像偏差和框内深度生成 `HOLD`、四向对准、`ADVANCE` 或 `STOP` 建议。建议逐帧写入报告且标记 `advisory_only`，运行器没有把建议转换成速度命令。源文件哈希现在覆盖检测器、相机提供器、多帧状态机和建议器，便于以后定位实验所用实现。

47 项离线测试全部通过。实飞 `mission_a_hit_20260909_113349_501160` PASS，并实际捕获一次销毁期间的残留 `BalloonTarget` 接触；该事件记为 `post_hit_target_contact_ignored`，没有重复计分，也没有中止撤离。DroneA 接近阶段深度从约 5.09 m 降至 1.18 m；6 个具备完整信息的帧均建议 `ALIGN_RIGHT`，另 6 帧因目标未确认建议 `HOLD`。这说明建议层可以被审计，也说明当前固定航线并未执行视觉对准。下一步应先设计受限速度和最大持续时间，再以显式开关做低速闭环测试。

## 2026-09-09：视觉建议阈值配置化

### 2026-09-10 视觉接近到碰撞交接完成

视觉补位首轮 `mission_visual_fallback_20260910_111546_020162` 在 A 的待命点卡住；该点不在已经验证的前视气球视场内，进程被安全停止并由 `recovery_20260910_111731_289884` 回收。修正入口先将 A 移到已通过的视觉观察点，再启动 A 的视觉漏击逻辑，避免把视场问题误判为目标丢失或补位失败。

第二次 `mission_visual_fallback_20260910_112216_485180` 已在正确观察点看到气球，但 A 仍未前进。检查发现把 `run.observe()` 的遥测耗时计入了相机帧龄，所有观测都超过0.5s时效门限。修复为只测相机请求时长，遥测耗时不再使视觉帧失效；该轮仍保留为 FAIL。

新增 `Run-Visual-Contact.cmd` 和 `visual_contact` 启动场景。流程先由视觉完成对准与2.5m距离停止，再调用已经验证的坐标接触路线完成最后一段；视觉只提供交接前置条件，最终仍必须有新的 `BalloonTarget` 碰撞事件，才允许销毁气球。

首轮 `mission_visual_range_20260910_110544_802509` 和修正后第二轮 `mission_visual_range_20260910_110807_534684` 暴露了阶段名称和坐标路线问题，均保留为 FAIL。回收记录 `recovery_20260910_111005_471132` PASS 后，第三轮 `mission_visual_range_20260910_111043_653565` PASS：视觉停止、DroneA 命中、气球移除、两机降落和API释放均通过，命中飞机 DroneA，48组视觉样本，最小间距4.0159m。完整离线测试仍为54项通过。当前已完成“视觉接近→碰撞权威判定”的单轮集成，下一步才适合做视觉版 A 漏击/B 补位。

### 2026-09-10 重复测试发现停止/居中条件冲突

视觉接触首轮 `mission_visual_range_20260910_110544_802509` FAIL。视觉停止条件通过后飞机正常向目标前进，但新阶段名没有匹配旧碰撞裁判的严格 `CONTACT` 条件，导致真实接触没有写入 `hit`，最终报告拒绝无权威碰撞证据。该失败是阶段命名集成错误，现场已暂停并保留；修正后再复测。

从新启动入口执行的 mission_visual_range_20260910_092707_212893 FAIL：测距2.373m已满足停止条件，但水平误差0.08389略超0.08。原逻辑停止所有轴，同时要求误差继续减小，形成等待直到超时。该失败不影响首轮结果真实性，但说明首轮不代表重复性已验收。

修复为达到2.5m停止边界后只禁止前进，允许低速横向/垂向校正。增加专门回归测试，54项离线测试通过。recovery_20260910_092938_137233 确认两机回收、解除武装和API释放后，重新从 visual_range 启动入口执行完整测试。用户操作说明见 docs/visual-demo-guide.md。

### 2026-09-10 09:26 对准修复与视觉测距停止通过

### 2026-09-10 视觉版双机补位闭环完成

首轮视觉补位 `mission_visual_fallback_20260910_112536_384454` FAIL：A 已完成视觉漏击和走廊清空，但 B 从 `y=+4` 直接进入前视控制，气球持续贴在图像左边缘并被 `truncated` 保护拒绝，90 秒后超时。该失败保留，之后由 `recovery_20260910_112847_958273` PASS 回收两机。

修正 `run_visual_fallback.py`：B 现在和 A 一样先移动到已经验证的视觉观察点 `[-5.0, +0.5, -3.0]`，确认实际位置后才进入视觉接近。修正后的完整实飞 `mission_visual_fallback_20260910_112931_003301` PASS：A 记录 47 个视觉接近样本后按测试策略漏击；系统记录 `corridor_clear` 和 `fallback_ready`，B 记录 55 个视觉接近样本并通过距离停止条件；随后 B 产生新的 `BalloonTarget` 碰撞，命中证据明确为 `DroneB`，气球被移除。两机均落地、解除武装并释放 API 控制，最终状态 `HIT_CONFIRMED`，最小间距 3.7932 m。

本轮证明的是“视觉观察/对准/距离停止 → A 漏击事件 → B 自动接替 → 碰撞权威命中 → 安全收尾”的双机闭环。视觉检测目前使用 RGB 颜色/轮廓候选和 AirSim `DepthPerspective` 框内中位深度；视觉负责接近和交接门控，碰撞事件仍是命中裁判。当前尚未接入 YOLO/SAM，也未宣称 RGB 与深度严格时间同步或三维定位精度。修正后离线测试 54 项全部通过，Python 字节码编译通过。

### 2026-09-10 视觉最终接近试验记录

将 B 的最后 2.5 m 改为视觉前进后，连续三轮均按安全超时退出：前两轮由残余垂向控制使目标离开视场，第三轮确认普通速度 RPC 本身不保持高度。失败目录分别为 `mission_visual_fallback_20260910_114207_144848`、`mission_visual_fallback_20260910_114703_724226` 和 `mission_visual_fallback_20260910_115750_165640`，均已暂停并回收。当前代码已加入悬停后的高度反馈项，离线 55 项测试通过；下一次实飞需验证高度保持后，才能把“2.5 m 到碰撞”标记为 PASS。此前的视觉接近→碰撞混合闭环 PASS 仍然有效，本段视觉最终攻击尚未验收。

独立 altitude_probe_20260910_092222 验证 moveByVelocityZ 的本地NED高度响应：保持阶段漂移约0.005m，上移指令0.3m后实际约0.30m，恢复原高度也通过。该证据表明高度位置环可用，不证明此前所有漂移的单一根因。

运行器改用持续高度设定值，视觉垂直误差只缓慢改变该值；锁定偏航为0，减少同一循环内重复取图，记录姿态、世界坐标及采样耗时。mission_visual_alignment_20260910_092333_834526 PASS，7帧内水平误差0.1161→0.0712，垂直误差0.0042→-0.00036，高度变化约0.011m，两机降落并释放控制。

增加 --range 显式开关：稳定且未裁剪的检测、有效深度及不超过0.5s的取图耗时才允许前进；最高0.15m/s，测距≤2.5m即停止，连续三帧误差≤0.08且达到距离阈值后返回降落。工作空间、高度变化和90s超时均有限制。mission_visual_range_20260910_092458_884439 PASS：35帧，初始距离3.4531m、最终2.4297m，最终水平/垂直误差0.07855/0.00728，最小双机距离3.7518m，两机降落和API释放均验证。人工查看最终标注图，气球完整可见。

新增 Run-Visual-Range.cmd 与启动器 visual_range 场景。53项离线测试通过，新启动器参数检查通过。当前为混合视觉控制：准备航点、定高反馈和安全边界用仿真状态，横向对准与前进停止用相机；未完成RGB/深度严格同步或三维定位精度标定，也尚未接入视觉版双机接触任务。

### 2026-09-10 检查与测距入口构建

检查时 UE 已关闭，重新启动后场景 scene_20260910_091737_083559 通过。上次对准遥测高度从 -2.994m 变为 -2.268m，提示下降。新增取图前后 hover、采样耗时和姿态记录，将对准超时设为60s；mission_visual_alignment_20260910_091747_643821 再次超时 FAIL，高度仍发生漂移，不能将原因归结为缺少 hover。两机现场已暂停，未自动复飞。

开始下一部分的 visual_range_gate.py：仅接受0～0.5s的新观测，稳定检测与有效距离才能产生速度，近距离和缺失数据保持零速度；尚未接入真实前进。新增2项测试，完整53项通过。这些离线测试不构成闭环验收。后续需要独立定高/垂直响应试验定位控制问题，再启用测距前进。

### 19:40 首次视觉对准闭环：超时，保留现场

UE 重启后单相机取图约 0.078 秒，setup_scene 通过。暂停渲染验证加入三帧推进，并从实际 world/local 位姿推导坐标偏移；servo_view_20260909_193922 显示完整气球，中心水平误差 -0.0017，偏移观察点误差 +0.1260。已查看标注图。

新增 run_visual_alignment.py，仅以图像偏差输出横向/垂向短时机体系速度，前进速度固定为零；带 1m 空间范围、25s 超时、目标丢失保护，成功后调用原有降落流程。51 项已有离线测试通过，但不替代新运行器实飞验收。

mission_visual_alignment_20260909_194006_171129 实飞 FAIL：23 帧记录中水平偏差约从 0.176 减至 0.119，垂直偏差却增大，25s 超时触发。仿真已暂停，两机仍在空中，未解除武装。下一步需要排查姿态/图像垂直误差及短指令与采样周期的关系；不得将本轮视为闭环成功，也不得直接重跑地面启动入口。保留 report.json、telemetry.jsonl 和逐帧标注图。

### 19:36 观察点验证中断

新增 check_servo_view.py，用暂停场景中的临时位姿测试中心/偏移观察点，并在 finally 恢复位姿和暂停状态。实际运行中，setup_scene 在 simGetImage 请求超时，UE 随后退出（Blocks.log 记录 RequestExitWithStatus(1,3)，尚未确定根因）；观察点脚本在初始 RPC 检查失败，没有执行位姿移动或飞行命令。已发出 UE 重启命令，原失败日志保留。本轮不能声称取得观察点图像或完成闭环实飞。

### 后续：低速视觉控制策略初版

重新启动 Blocks 游戏窗口并确认 UE 进程和 RPC 可用。新增 visual_servo.py，计算相机对齐的机体系 NED 速度；横向/垂向各限 0.15 m/s，前进限 0.15 m/s，单指令 0.2 s，距离小于等于 2 m 优先输出零速度。目标丢失、裁剪、无效或非有限数据也输出零速度。该模块尚未接入实际 RPC 飞行；51 项离线测试通过。

使用 replay_visual_servo.py 回放 mission_a_hit_20260909_113349_501160 的 DroneA 观测，65 帧全部为 UNRELIABLE_OBSERVATION，输出保存为该目录 visual_servo_replay.json。现有数据未提供满足严格输入条件的帧，故本轮不宣称视觉闭环已通过。下一步必须验证完整目标的观察点、相机视场与姿态，再接入时效检查、空间边界和总超时的运行器。UE 已打开，未执行新的起飞命令。

将对准死区 `alignment_deadband=0.10` 和停止距离 `stop_distance_m=1.50` 移入 `configs/mission.json`，由 `MissionConfig` 校验后传给建议器，任务报告也保存实际阈值。非法死区（不在 0～1 内）或非正停止距离会在起飞前拒绝。完整离线测试增至 48 项并通过，Python 全量字节码编译通过。该修改只改变建议记录参数来源，没有改变飞行命令；由于当前 5 小时额度达到 99%，本轮不再启动约一分钟的实飞，避免在飞行中途触发额度中断。现场沿用上一轮安全状态：仿真暂停、气球已恢复、两机落地且 API 控制释放。

### 2026-09-10：纠正最终段 RPC 与日志不一致，复核场景

发现 run_visual_fallback.py 计算并记录 vz，但 moveByVelocityAsync 的第三个实参硬编码为 0.0。此前改变反馈正负号的试验没有发送该修正，不能用来判断反馈方向。已改为 send_final_velocity 发送完整记录向量，加入 Mock RPC 回归测试（正、零、负垂向指令），56 项测试通过。NED 的正 Z 为向下；此前把 Z 增大称为上漂的说明有误。删除未经验证的固定负向刹车，并加入 0.5 m 高度偏离终止条件；最终段时限调整为 120 秒，仍需实飞验证。

实飞启动检查发现当前资源名称为 sphere/cylinder，已通过唯一的不区分大小写匹配解析实际资源名。scene_20260910_154246 检查在目标碰撞体素采样处失败（No blocking geometry detected around the target）；未执行起飞。完整目录名以 logs 内实际记录为准。控制器修复的飞行效果为 NOT_RUN，不能声称视觉最终命中通过。下一步先修复当前 UE 气球碰撞资源，重新通过场景检查，再运行定高和最终接近试验。

### 2026-09-10：碰撞资源隔离诊断

补充验证：UE 自动关闭原因已确认是 `SizeProbe` 同名 Actor 重复生成，已回收并用独立 `-game` 方式重启。将 `settings.json` 前视相机 FOV 调为 120° 后，近距离静态图像重新检测到完整气球；`probe_near_camera_20260910_160916` 保存了 current/wide/forward_wide 三组图像。`mission_visual_fallback_20260910_162901_375361` 完成 A 漏击、B 补位及视觉最终段，B 高度约 -2.991m、目标持续检测、末端深度约 1.04m，但 60s 超时未碰撞；`recovery_20260910_163422_785204` PASS。最终段当前瓶颈是前进距离/碰撞几何，而不是相机丢失或高度漂移，仍不能宣称纯视觉命中通过。

对当前注册资源进行独立生成/体素采样/删除探针，结果保存 logs/collision_asset_probe.json：sphere 和 cylinder 均为 0；EditorSphere 为 7999、EditorCylinder 为 6780。后两者单位缩放下几乎填满 2 m 采样区域，不能直接代替原气球资源。尝试进一步标定 EditorSphere 缩放时 simSpawnObject RPC 超时，finally 删除也超时；本轮未执行飞行，SizeProbe 是否生成和清理未确认。不得宣称场景恢复、碰撞验证或视觉最终接近通过。下一次首先恢复 RPC 并检查 SizeProbe 残留，再使用准确来源的 BasicShapes 资源修复注册名称冲突。

### 2026-09-10：崩溃定位、独立游戏恢复与最终段定高

Blocks.log 明确记录 UE Fatal Error：SizeProbe 同名 Actor 已存在。上一轮尺寸探针重复命名触发崩溃；以后探针必须使用唯一名称，不能依赖刚删除对象立刻可复用名称。重启为独立 -game 后 scene_20260910_154834_777992 场景检查 PASS，阻挡体素 1285；编辑器模式资源名映射与独立模式不同。

mission_visual_fallback_20260910_154835_598087 实际发送垂向修正后仍触发 0.5m 高度偏差保护，回收 recovery_20260910_155329_103987 PASS。最终段改为读取本机 NED 高度作为 moveByVelocityZAsync 设定值，并在取图前保持该高度。mission_visual_fallback_20260910_155410_048036 最终段高度保持约 -2.992m，末帧深度 0.978m，目标完整且 DETECTED；120秒超时未命中，因此整轮仍 FAIL。此证据支持该轮定高改善，不代表视觉接触成功。

新增 resume_visual_contact_probe.py，用暂停且仍有双机 API 控制的现场隔离测试剩余最终段；其状态机初始化为诊断用途，不能视为完整 A/B 端到端验证。
### 2026-09-10：最终验收通过

`mission_visual_fallback_20260910_170402_075141` PASS：A 完成视觉接近后按测试分支漏击，B 自动补位；B 视觉阶段记录 44 帧，最终视觉记录 39 帧，目标检测和高度保持有效。近距离视觉冲刺后发生遮挡，系统按设计切换到已验证的短坐标接触收尾；新的 `BalloonTarget` 碰撞明确命名 DroneB，随后气球移除。两机均落地、解除武装并释放 API 控制，最小间距 4.0056m，最终状态 `HIT_CONFIRMED`。当前验收版本是“视觉门控接近 + 碰撞权威收尾”；纯视觉从 1m 内持续到碰撞仍属于后续视觉识别升级项。

### 2026-09-11：视觉补位接入批量验证

`batch_validate.py` 新增 `visual_fallback` 场景，逐轮恢复中心目标并调用视觉补位运行器；任一任务失败即停止并保留现场。批量行现在记录 A、B 和 B 最终段的视觉帧数、漏击到派发时间、最终段模式及视觉补位验收标志；总汇总区分纯视觉碰撞和视觉门控后的坐标接触收尾。目标偏移与漏击偏移在视觉批量模式中明确拒绝，避免将尚未标定的观察点误作已支持能力。离线回归测试增至 58 项并全部通过。

真实冒烟批次 `batch_20260911_114941_572017` PASS，对应任务 `mission_visual_fallback_20260911_114943_368764`：A、B 和 B 最终段分别记录 41、43 和 41 帧；A 漏击到 B 派发 11.515 秒；DroneB 的新 `BalloonTarget` 碰撞得到确认；最小双机间距 4.0357 m，最大待命漂移 0.1644 m；两机落地、解除武装并释放 API 控制。最终段模式为 `visual_gate_coordinate_contact`，因此本轮增加的是批量执行链路与第二个完整视觉补位 PASS，不构成纯视觉接触通过。

### 2026-09-11：有界遮挡冲刺直接碰撞通过

对冒烟轮和后续三轮的 B 最终段逐帧复核显示：视觉冲刺在 `x≈-2.29` 开始，目标在 `x≈-1.49` 因贴近图像边缘被标记为裁剪，旧逻辑随即把速度清零；已验证的坐标收尾通常在 `x≈-1.11` 发生碰撞。四个旧策略视觉批次均 PASS，但最终段全部为 `visual_gate_coordinate_contact`。最低视觉深度曾达到约 0.27 m，这排除了“尚未接近”的单一解释。

最终段改为视觉锁定后的有界承诺冲刺：目标在 1.5 m 内且水平居中时记录冲刺原点；随后即使机体导致近距离遮挡，也以 0.35 m/s 保持直行，最大累计位移 1.35 m，原有 15 秒时限、高度保持、双机间距检查和碰撞检查继续生效。超距或超时仍抛出 `TimeoutError`，并由现有坐标接触路径安全收尾。记录新增 `dash_travel_m` 和 `FINAL_DASH_OCCLUDED`，避免把遮挡期间描述为持续视觉观测。两项离线边界测试加入后，完整回归为 60 项通过。

首轮 `batch_20260911_121136_737764` PASS，任务 `mission_visual_fallback_20260911_121138_406002` 在 4 个遮挡帧后直接碰撞，冲刺最大位移 1.1474 m，模式为 `visual_collision`，未进入坐标接触。后续批次 `batch_20260911_121603_945486` 3/3 PASS，三轮均为 `visual_collision`、坐标收尾 0 次；冲刺最大位移分别约 0.97、1.12 和 1.14 m，批次最小双机间距 3.9884 m，最大待命漂移 0.1619 m，平均 A 漏击到 B 派发 11.896 秒。四轮均确认 DroneB 新目标碰撞、气球移除、双机落地、解除武装和 API 释放。该结果证明中心固定目标下“视觉锁定 + 有界遮挡冲刺”的四轮重复通过，不等同于目标全程可见的纯视觉制导，也不外推到偏移目标或新环境。

### 2026-09-11：机载传感器遥测第一阶段

`configs/settings.json` 为 DroneA 和 DroneB 显式配置命名的 IMU、气压计、GPS、磁力计、前向距离和下向距离传感器；前向传感器位于机体前方 1 m，下向传感器按 AirSim 文档使用 `Pitch=-90`。新增 `onboard_sensors.py`，将六类 RPC 输出转换为统一 JSON 快照，记录原始时间戳、数值有效性、新鲜度、饱和/低于标称最小距离标志和累计错误计数。任务运行器按约 1 Hz 把双机快照写入 `telemetry.jsonl`，并把每机每传感器的 samples/valid/fresh/errors 汇总写入 `report.json`。本阶段明确为 `advisory_only`，不改变飞行控制或状态机终止条件。

配置安装并重启独立 Blocks 后，`check_onboard_sensors.py` 一次性检查 PASS，双机 12 路命名传感器均可访问且时间戳新鲜。AirSim 1.8.2 的前向无命中距离会略高于声明的 20 m 最大值（实测约 20.10–20.25 m），因此在 1 m 明确容差内记录为 `saturated=true`，而不是误报故障；超出该容差仍无效。下向静止距离实测约 0.29–0.44 m。

集成实飞 `mission_a_hit_20260911_143929_567908` PASS：DroneA 命中、气球移除、双机落地并解除武装。任务写入 202 条遥测，其中 57 条携带完整双机传感器快照，覆盖 TAKEOFF、STAGING、APPROACH、CONTACT、RETREAT、RETURN 和 LANDING。共 684 路样本全部 fresh，RPC errors 为 0；683 路数值 valid。唯一无效值是 CONTACT 阶段 DroneA 前向距离 `-0.0499 m`，发生在传感器安装点进入目标碰撞体时，保留为无效而不放宽负距离规则。离线测试增至 67 项并全部通过。该实验证明记录与健康分类链路可运行，不证明传感器已经参与飞行控制或故障处置。
