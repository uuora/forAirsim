# 小模型与语言数据启动记录

## 已实现

2026-09-13，本机检查为RTX 3070 Laptop 8GB。AirSim Python环境没有torch/transformers。
本次下载官方Qwen3-0.6B Q8_0 GGUF和llama.cpp b10941 Windows CPU运行包；不修改现有Python依赖。
模型及下载来源、版本、SHA256见 `downloads/local_llm/manifest.json`。模型许可证保存在同目录MODEL_LICENSE。

这是已预训练的小型语言模型，没有进行领域微调。CPU后端4线程，4096上下文，关闭思考，temperature=0，seed=42，最多输出180 tokens。
本次未测试GPU、Jetson或LLM驱动的仿真飞行。LLM入口只输出计划预览，没有飞行执行开关。

## 运行

在D:/forAirsim打开PowerShell：

```powershell
# 首次安装或复现下载
& .\.venv\python.exe python/setup_local_llm.py

# 启动本地模型、推理一条指令、保存报告，随后自动结束模型服务
& .\.venv\python.exe python/run_language_planner.py --instruction "让A对准中心红气球，然后返回降落。"

# 运行当前8条开发冒烟样本
& .\.venv\python.exe python/run_language_planner.py --evaluate
```

服务器只绑定127.0.0.1。指令在本机处理。首次运行需下载模型；后续推理无需云端模型API。
当前输出只能选择现有固定中心气球的visual_alignment/visual_range，或CLARIFY/UNSUPPORTED。
计划预览会显式列出两机起降、B待命。未知位置搜索、自由航线和多机任务规划不属于当前支持能力。

## 首轮真实模型结果

报告：`logs/language_planner_20260913_193332_164961/report.json`。
8条AI候选标注的smoke_eval中：6条通过结构与字段一致性检查，3条与候选答案完全一致。
请求耗时约1.37～2.81秒，包含本地请求/推理/响应，不含服务启动；后续请求使用了提示词缓存。
该结果仅是小样本开发诊断，不是论文性能结论，也不是飞行成功率。

主要错误：复合的“对准再靠近”被简化为仅对准；不支持的颜色/速度要求产生自相矛盾字段；附加区域或接触要求被忽略。结构校验拦截了两条不一致计划，但不能发现所有语义错误。
不修改预期标签来迎合模型，不将错误输出混入正确训练答案。

单条中文指令“让A对准中心红气球，然后返回降落。”已正确解析；报告为 `logs/language_planner_20260913_193532_274509/report.json`，无提示缓存的请求耗时约2.99秒。
本次完整离线回归83项通过；新增检查覆盖计划字段、附加代码拒绝、状态一致性、待澄清不产生动作以及数据组不跨划分。这些检查不代表自然语言语义已可靠。

## 数据与下一步

`datasets/language_tasks_v0/seed.jsonl`共26条候选样本：18条development，8条smoke_eval。全部等待人工审核，没有自动标记为已审核。
下一步先审核能力表/标签，扩充约200条真实表达和错误案例，建立规则解析对照与独立人工测试集。
随后才决定是否做LoRA、扩大至1000～3000条候选训练数据，并比较原模型、提示词基线、微调模型。
误解附加要求的问题得到充分验证前，计划预览与飞行执行保持分离。

## 来源与披露

- 官方模型：https://huggingface.co/Qwen/Qwen3-0.6B-GGUF
- 官方运行器：https://github.com/ggml-org/llama.cpp/releases/tag/b10941
- 本地实验报告、下载manifest和源码记录支持上述运行事实。
- 本文与种子数据由AI辅助生成；尚未经用户/导师人工审核。没有声称完成训练、Jetson部署或获得新算法贡献。
